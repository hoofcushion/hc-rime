# 引用通用配置文件：
__include: ts__detail.yaml:/
# 引用特殊配置文件：
__include: _schema_settings/ts_triple.yaml:/map
switches: {__include: _schema_settings/ts_triple.yaml:/switches}
# 引用方案快捷键：
__patch:
 key_binder/bindings/+: {__include: _schema_settings/ts_triple.yaml:/array/bindings}
schema:
 __include: _schema_settings/_global:/schema
 __patch:
  author/@before 0: 阿森纳里 15678407
 schema_id: ts_triple
 name: 『三拼』
 description: |
  蹄垫系列方案之三拼
  拼写运算来自 阿森纳里 三码拼音
  由蹄垫改进适配 Qwerty 布局
__patch:
 schema/dependencies/+:
  - ts_en # 英文
  - module_fnua_triple # 反查
__patch:
 # - engine/processors/@before 0:     "predictor"
 # - engine/translators/@before 0:    "predict_translator"
 - engine/segmentors/@after 1:      "affix_segmentor@module_fnua_triple"
 - engine/processors/@after 1:      "lua_processor@*reverse_pro*processor@module_fnua_triple"
 - engine/segmentors/@before 1:     "affix_segmentor@module_fnua_triple"
 - engine/translators/@before last: "lua_translator@*ts_triple.reverse*translator@module_fnua_triple"
 - engine/translators/@after last:  "lua_translator@*ts_fixed*translator@translator"
 - engine/translators/@after last:  "lua_translator@*ts_en*translator@module_en"
 - abc_segmentor/extra_tags/@after last: "ts_en"
 - ts_triple.custom:/patch?
translator:
 initial_quality: 1
 dictionary: ts_triple
 user_dict: ts_triple
 syllable_len: 3
module_en:
 __include: ts_en.schema.yaml:/translator
 tag: ts_en
module_fnua_triple:
 prefix: "`"
 trigger: "space"
 tag: module_fnua_triple
 tips: 〔拼音反查〕
 dictionary: module_fnua_triple
 spelling_hints: 99
 enable_user_dict: false
recognizer:
 patterns:
  module_fnua_triple: "`.*$"
